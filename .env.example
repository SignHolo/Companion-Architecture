# Environment variables for Story Weaver
# Copy this file to .env.local and customize for your environment

# Development server IP - set this to your computer's IP address on your local network
# Find your IP with `ipconfig` (Windows) or `ifconfig` (Mac/Linux)
EXPO_DEV_SERVER_IP=192.168.1.100

# Domain for deployed environments
EXPO_PUBLIC_DOMAIN=

# Port for the API server (should match server configuration)
API_PORT=5000